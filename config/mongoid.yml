development:
  sessions:
    default:
      database: chromessengerbackend_development
      hosts:
        - localhost:27017
      options:
  options:
production:
  sessions:
    default:
      uri: <%= ENV['MONGO_URL'] %>
      options:
        max_retries: 1
        retry_interval: 0
